
﻿# Scoper

for linux
sudo apt install libpcap-dev
pip install scapy

The exe file is in dist folder

﻿# Scoper

for linux
sudo apt install libpcap-dev
pip install scapy

The exe file is in dist folder

